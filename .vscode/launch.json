{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Waveshare MQTT",
            "type": "debugpy",
            "request": "launch",
            "module": "waveshare_mqtt.main",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
            "justMyCode": true
        }
    ]
}
